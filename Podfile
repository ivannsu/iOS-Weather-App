platform :ios, '9.0'

target 'iOS Weather App' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for iOS Weather App
    pod 'SwiftyJSON'
    pod 'Alamofire'
    pod 'SVProgressHUD'
end
